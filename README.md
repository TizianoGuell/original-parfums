# Original Parfums

Frontend-only demo landing page for the brand concept.

## Project structure

- `frontend/`: React landing page.
- `vercel.json`: root deployment config that builds only `frontend/`.

## Commands

From `frontend/`:

- `npm start`
- `npm run build`
